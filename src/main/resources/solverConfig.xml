<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <solutionClass>com.burtleburtle.jenny.domain.JennySolution</solutionClass>
    <entityClass>com.burtleburtle.jenny.domain.TestCase</entityClass>
    <entityClass>com.burtleburtle.jenny.domain.TestCell</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>com.burtleburtle.jenny.solver.JennyConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Phase 1: Consolidation. Tabu Search with custom moves that target
         test-suite minimization directly. Runs up to 60s but exits early if
         no improvement in 30s, giving the search time to escape local optima
         via the merge moves when it is still making progress. -->
    <localSearch>
        <termination>
            <secondsSpentLimit>60</secondsSpentLimit>
            <unimprovedSecondsSpentLimit>30</unimprovedSecondsSpentLimit>
        </termination>
        <unionMoveSelector>
            <!-- Baseline T28 weights. S8 A/B tested 50/20/10/10/10 (5.0/2.0/
                 1.0/1.0/1.0) and the benchmark regressed (106-108 active, 1
                 uncovered vs baseline 106-108 active, 0 uncovered). The shrink
                 moves carry their weight on this problem; keep them at 3.0. -->
            <changeMoveSelector>
                <fixedProbabilityWeight>2.5</fixedProbabilityWeight>
                <entitySelector>
                    <entityClass>com.burtleburtle.jenny.domain.TestCell</entityClass>
                </entitySelector>
            </changeMoveSelector>
            <changeMoveSelector>
                <fixedProbabilityWeight>1.5</fixedProbabilityWeight>
                <entitySelector>
                    <entityClass>com.burtleburtle.jenny.domain.TestCase</entityClass>
                </entitySelector>
            </changeMoveSelector>
            <moveIteratorFactory>
                <fixedProbabilityWeight>1.5</fixedProbabilityWeight>
                <moveIteratorFactoryClass>com.burtleburtle.jenny.solver.RandomizeRowMoveIteratorFactory</moveIteratorFactoryClass>
            </moveIteratorFactory>
            <moveIteratorFactory>
                <fixedProbabilityWeight>3.0</fixedProbabilityWeight>
                <moveIteratorFactoryClass>com.burtleburtle.jenny.solver.DeactivateRedundantMoveIteratorFactory</moveIteratorFactoryClass>
            </moveIteratorFactory>
            <moveIteratorFactory>
                <fixedProbabilityWeight>3.0</fixedProbabilityWeight>
                <moveIteratorFactoryClass>com.burtleburtle.jenny.solver.MergeTestsMoveIteratorFactory</moveIteratorFactoryClass>
            </moveIteratorFactory>
        </unionMoveSelector>
        <acceptor>
            <entityTabuSize>7</entityTabuSize>
        </acceptor>
        <forager>
            <acceptedCountLimit>10</acceptedCountLimit>
        </forager>
    </localSearch>

    <!-- Phase 2: Refinement. Hill Climbing over single-variable moves
         (acceptorType=HILL_CLIMBING below). Strict-improvement acceptor —
         coverage Phase 1 produced cannot regress in Phase 2. -->
    <localSearch>
        <termination>
            <secondsSpentLimit>50</secondsSpentLimit>
            <unimprovedSecondsSpentLimit>20</unimprovedSecondsSpentLimit>
        </termination>
        <unionMoveSelector>
            <changeMoveSelector>
                <entitySelector>
                    <entityClass>com.burtleburtle.jenny.domain.TestCell</entityClass>
                </entitySelector>
            </changeMoveSelector>
            <changeMoveSelector>
                <entitySelector>
                    <entityClass>com.burtleburtle.jenny.domain.TestCase</entityClass>
                </entitySelector>
            </changeMoveSelector>
            <moveIteratorFactory>
                <moveIteratorFactoryClass>com.burtleburtle.jenny.solver.RandomizeRowMoveIteratorFactory</moveIteratorFactoryClass>
            </moveIteratorFactory>
        </unionMoveSelector>
        <acceptor>
            <acceptorType>HILL_CLIMBING</acceptorType>
        </acceptor>
        <forager>
            <acceptedCountLimit>1</acceptedCountLimit>
        </forager>
    </localSearch>

    <!-- Phase 3: Feasibility repair. A short Tabu Search over single-variable
         change moves. Unlike Phase 2's strict HILL_CLIMBING, the tabu acceptor
         takes the best non-tabu move each step even when it worsens the score,
         and the entity tabu forbids immediately re-touching the row it just
         changed — so it commits to breaking the last residual respectWithouts
         violation and then works elsewhere to re-cover, instead of oscillating
         on the stuck row. Paired with the 2-hard respectWithouts weight (see
         JennyConstraintProvider): breaking the Without is a strict hard-score
         improvement (-2 -> -1), so the repair holds as best and the search
         re-covers from there. Terminates the moment the best solution is
         feasible (0 hard), or after a short unimproved budget, so it costs
         little wall time when Phase 2 already produced a feasible solution.
         Total budget stays well within the oracle's 130s cap. -->
    <localSearch>
        <termination>
            <secondsSpentLimit>35</secondsSpentLimit>
            <unimprovedSecondsSpentLimit>20</unimprovedSecondsSpentLimit>
            <bestScoreFeasible>true</bestScoreFeasible>
        </termination>
        <unionMoveSelector>
            <changeMoveSelector>
                <entitySelector>
                    <entityClass>com.burtleburtle.jenny.domain.TestCell</entityClass>
                </entitySelector>
            </changeMoveSelector>
            <changeMoveSelector>
                <entitySelector>
                    <entityClass>com.burtleburtle.jenny.domain.TestCase</entityClass>
                </entitySelector>
            </changeMoveSelector>
        </unionMoveSelector>
        <acceptor>
            <entityTabuSize>5</entityTabuSize>
        </acceptor>
        <forager>
            <acceptedCountLimit>5</acceptedCountLimit>
        </forager>
    </localSearch>
</solver>
